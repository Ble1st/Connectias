# Next Steps

## High Priority
- [x] **Compose Migration Setup**: Enable Compose in `common` and `app` modules.
- [x] **Compose Infrastructure**: Create `ConnectiasTheme`, `Color`, `Type` in `common`.
- [x] **Compose Configuration**: Enable Compose in all feature modules (`device-info`, `network`, `privacy`, `security`, `utilities`).
- [x] **Compose Migration - Security**: Migrate `SecurityDashboardFragment` to `SecurityDashboardScreen`.
- [x] **Compose Migration - Network**: Migrate `NetworkDashboardFragment` to `NetworkDashboardScreen`.
- [x] **Compose Migration - Privacy**: Migrate `PrivacyDashboardFragment` to `PrivacyDashboardScreen`.
- [x] **Compose Migration - Device Info**: Migrate `DeviceInfoFragment` to `DeviceInfoScreen`.
- [x] **Compose Migration - Utilities**: Migrate `UtilitiesDashboardFragment` to `UtilitiesDashboardScreen`.

## Medium Priority
- [ ] Review and update dependencies in `libs.versions.toml`.
- [ ] Implement unit tests for new Compose components.
- [ ] Continue migration of detail screens (e.g., `WifiAnalyzer`, `PermissionsAnalyzer`).

## Low Priority
- [ ] Evaluate adopting Compose Navigation (after screen migration).

---
*Created on Nov 28, 2025*
