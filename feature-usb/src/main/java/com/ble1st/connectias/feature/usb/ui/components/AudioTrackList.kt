package com.ble1st.connectias.feature.usb.ui.components

// AudioTrackList has been migrated to feature-dvd.
// This file is kept temporarily to avoid build breakage if there are lingering references,
// but the content is commented out to remove dependencies on feature-dvd.

/*
import androidx.compose.foundation.layout.*
import androidx.compose.material.icons.Icons
import androidx.compose.material.icons.filled.PlayArrow
import androidx.compose.material3.*
import androidx.compose.runtime.Composable
import androidx.compose.ui.Alignment
import androidx.compose.ui.Modifier
import androidx.compose.ui.unit.dp
import com.ble1st.connectias.feature.dvd.models.AudioTrack

@Composable
fun AudioTrackList(
    tracks: List<AudioTrack>,
    onTrackSelected: (AudioTrack) -> Unit,
    modifier: Modifier = Modifier
) {
    // ... implementation removed ...
}
*/
