package com.ble1st.connectias.core.model

/**
 * Represents network connection types.
 * Pure Kotlin enum without Android dependencies.
 */
enum class ConnectionType {
    WIFI,
    MOBILE,
    ETHERNET,
    UNKNOWN
}
