<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions for Privacy features -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Permissions for Network features -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- ACCESS_FINE_LOCATION required for GPS/GNSS on all Android versions -->
    <uses-permission
        android:name="android.permission.ACCESS_FINE_LOCATION"
        tools:remove="android:maxSdkVersion" />
    
    <!-- Plugin Permissions - Commonly used by plugins -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <!-- Android 13+ granular media permissions -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    
    <!-- Network Permissions for plugins -->
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    
    <!-- Dynamic Receiver Registration Security -->
    <!-- DYNAMIC_RECEIVER_NOT_EXPORTED prevents exported dynamic broadcast receivers
         This is a security measure for Android 13+ (API 33+) that ensures dynamically
         registered receivers cannot be accessed by other apps unless explicitly marked as exported.
         This protects against unauthorized apps sending broadcasts to our receivers. -->
    <uses-permission android:name="android.permission.DYNAMIC_RECEIVER_NOT_EXPORTED"
        tools:ignore="ProtectedPermissions" />
    
    <application
        android:name=".ConnectiasApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App.Starting">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|smallestScreenSize|keyboardHidden|screenLayout">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity 
            android:name=".SecurityBlockedActivity"
            android:exported="false"
            android:theme="@style/Theme.Connectias"
            android:launchMode="singleTask"/>
        
        <!-- Baseline Profile Components -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.startup"
                android:value="androidx.profileinstaller.ProfileInstallReceiver" />
        </provider>
        
        <receiver
            android:name="androidx.profileinstaller.ProfileInstallReceiver"
            android:exported="false"
            android:permission="android.permission.DUMP"
            tools:replace="android:exported" />
        
        <!-- Hardware Bridge Service - Provides hardware access to isolated sandbox -->
        <!-- Runs in main process with all app permissions -->
        <service
            android:name="com.ble1st.connectias.hardware.HardwareBridgeService"
            android:enabled="true"
            android:exported="false" />
        
        <!-- Plugin Sandbox Service - Runs in SEPARATE process for crash isolation -->
        <!-- isolatedProcess="true" = TRUE OS-Level isolation (no app permissions) -->
        <!-- Security: Hardware access controlled via Hardware Bridge + PermissionManager -->
        <service
            android:name="com.ble1st.connectias.core.plugin.PluginSandboxService"
            android:process=":plugin_sandbox"
            android:isolatedProcess="true"
            android:exported="false" />
    </application>

</manifest>