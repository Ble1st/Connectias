<?xml version="1.0" encoding="utf-8"?>
<!--
    Network Security Configuration for Connectias
    
    This configuration enforces:
    - No cleartext traffic by default
    - System certificate trust
    - Certificate pinning for configured domains
    
    Note: Certificate pins should be updated with actual certificate hashes
    before production deployment.
-->
<network-security-config>
    <!-- Base configuration: No cleartext traffic allowed -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Trust system CA certificates -->
            <certificates src="system"/>
        </trust-anchors>
    </base-config>

    <!-- Debug configuration: Allow localhost for development -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system"/>
            <certificates src="user"/>
        </trust-anchors>
    </debug-overrides>

    <!-- 
        Domain-specific configurations with certificate pinning.
        Uncomment and configure with actual pins for production use.
    -->
    
    <!--
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">api.example.com</domain>
        <pin-set expiration="2025-12-31">
            <pin digest="SHA-256">AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=</pin>
            <pin digest="SHA-256">BBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBB=</pin>
        </pin-set>
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </domain-config>
    -->

    <!-- Allow local network for development and network scanning features -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.0.0</domain>
        <domain includeSubdomains="true">172.16.0.0</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
</network-security-config>
